# 蝶威量化因子挖掘挑战赛 - 参赛指南

## 一、比赛是什么？（通俗版）

简单来说，这是一个**量化投资比赛**。你需要做的就是：

> **用 A 股的历史交易数据（股价、成交量、买卖盘口等），算出一个"分数"（因子），来预测哪些股票明天可能涨得更好。**

具体流程：
1. 平台给你沪深300成分股的**分钟级别高频交易数据**（2023-2024年两年的数据）
2. 你写代码把这些数据处理成每 15 分钟一个的"因子值"
3. 平台用你的因子去模拟买卖股票，看能不能赚到超越大盘的收益
4. 赚得越多、越稳定，你的排名就越靠前

**你不需要真的去炒股**，只需要写一个 Python 函数 `main(datasource, start_date, end_date)`，返回一个包含 `date`、`instrument`、`factor` 三列的表格即可。

---

## 二、官方 Demo 的逻辑

官方提供的 demo (`demo/demo.ipynb`) 实现了一个**订单簿压力因子**，逻辑如下：

1. **计算中间价**: `mid_price = (卖一价 + 买一价) / 2`
2. **计算加权买卖量**: 对盘口 1~5 档的挂单量做指数衰减加权（越靠近盘口权重越大）
3. **计算不平衡度**: `imbalance = (买方量 - 卖方量) / (买方量 + 卖方量)`
4. **结合价差调整**: 用不平衡度除以价差的平方根，得到"压力因子"
5. **标准化处理**: 在每个 15 分钟窗口内做 Z-Score 标准化
6. **波动率调整**: 如果市场波动太大，对因子值打 7 折（降低噪音）
7. **最终输出**: 用 `tanh` 函数限制因子到 (-1, 1) 范围，并取反向

核心思路：**买盘力量比卖盘力量强的股票，反而预期会下跌**（反转逻辑）。

---

## 三、其他策略分析

### 1. 价格排序因子 (`xyf/价格排序.ipynb`)
- **策略**: 在每个 15 分钟截面内，按价格排序，低价股得分高
- **优点**: 逻辑简单，计算快速
- **缺点**: 过于简单，低价效应可能被 BARRA 风格因子中和掉，实际增量贡献有限

### 2. 动量反转因子 (`xyf/动量反转.ipynb`)
- **策略**: 计算窗口内收盘价 vs 均价的偏离度，偏离越大反转信号越强
- **优点**: 捕捉短期均值回归效应，tanh 缩放处理异常值
- **缺点**: 只用价格信息，忽略了盘口和成交量等多维信息

### 3. 价格&反转复合因子 (`xyf/价格&反转复合.ipynb`)
- **策略**: 20% 低价因子 + 80% 均值回归动量因子
- **优点**: 多因子组合比单因子更稳健
- **缺点**: 低价因子可能与市值高度相关，被中性化后效果大打折扣

### 4. 订单簿压力因子 (`xyf/订单薄压力.ipynb`)
- **策略**: 和 Demo 几乎一致（因子方向相反：因子 ×1 而非 ×-1）
- **优点**: 利用了丰富的盘口信息（5档买卖数据），包含波动率自适应调整
- **缺点**: 单一维度（仅订单流），在极端行情下容易失效

### 5. 量价相关性因子 (`xyf/量价相关性.ipynb`)
- **策略**: 计算窗口内价格收益率与成交量变化的 Pearson 相关系数
- **优点**: 视角独特，与其他因子相关性低，能捕捉资金流向信息
- **缺点**: 样本量小时统计不稳定（窗口内快照数较少时相关系数波动大）

### 6. Python 版动量因子 (`xyf/momentum_py-v3.ipynb`)
- **策略**: 和"动量反转"相同逻辑，带完整回测引擎
- **优点**: 代码完整可直接运行，有分块处理和评估流程
- **缺点**: 同动量反转因子

---

## 四、我的最优方案

我的方案 (`mycode/my.ipynb`) 采用 **多维微观结构复合因子**，综合三个低相关性子因子：

| 子因子 | 权重 | 数据维度 | 捕捉的信号 |
|--------|------|----------|------------|
| 订单簿不平衡 (OBI) | 45% | 盘口5档数据 | 供需力量对比 |
| 均值回归 (MR) | 35% | 价格 | 短期反转效应 |
| 量价相关性 (PVC) | 20% | 价格+成交量 | 资金流向同步性 |

**为什么这个方案更好？**
1. **多维度信息融合**: 同时利用了价格、成交量、盘口三类数据，信息利用率更高
2. **因子正交性**: 三个子因子捕捉不同层面的信号，相关性低，组合后更稳健
3. **波动率自适应**: OBI 子因子包含波动率调整机制，高波动时降低信号强度
4. **全 SQL 实现**: 整个计算在 DAI 数据引擎中完成，速度快，内存占用低
5. **并行回测引擎**: 使用多线程分块处理，充分利用计算资源

---

## 五、你需要做什么？

### 快速开始步骤：

1. **注册账号**: 在合作的线上量化平台注册并获取比赛专用账号
2. **了解数据**: 熟悉 `cpt_dwc_2026_stock_hs300_snapshot` 数据表的字段含义
3. **运行 Demo**: 先运行 `demo/demo.ipynb` 确认环境正常
4. **开发因子**: 修改 `mycode/my.ipynb` 中的 `main()` 函数，尝试不同的因子构建逻辑
5. **本地验证**: 使用 `__main__` 中的分块回测引擎在本地验证因子效果
6. **提交代码**: 在平台上提交你的 Notebook，系统会自动运行并打分

### 注意事项：

- **只需要提交 `main()` 函数代码**，平台会自动调用
- 返回的 DataFrame 必须且只能包含三列：`date`、`instrument`、`factor`
- 因子值越大越好（平台默认因子方向），请确保因子方向正确
- CPU Notebook 运行时间不得超过 **9 小时**
- **禁止使用外部网络和未授权的外部数据**
- 每日因子覆盖率不低于 60%（缺失率 ≤ 40%）

### 评分标准：

```
最终得分 = 0.3 × 单因子分析排名 + 0.7 × 单因子回测排名
```

- 单因子分析看**多空组合夏普比率**
- 单因子回测看**超额收益**和**超额夏普**
- 会在 16 个不同的时间截面上取均值

### 关键时间节点：

| 阶段 | 时间 | 说明 |
|------|------|------|
| 报名 | 截止 2026-01-31 | 完成报名和组队 |
| 初赛 | 2026-01-26 ~ 2026-02-16 | 开发和提交因子，公榜排名 |
| 私榜 | 2026-02-16 ~ 2026-02-20 | 锁定代码，增量数据验证 |
| 决赛 | 2026-02-21 ~ 2026-03-01 | 前15名进入决赛答辩 |
| 颁奖 | 2026年3月初 | 答辩 + 颁奖典礼 |

祝你在比赛中取得好成绩！🏆
